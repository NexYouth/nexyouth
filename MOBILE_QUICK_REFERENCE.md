# 移动端适配 - 快速参考

## 🎯 核心特性

### 响应式断点
```
- 桌面: > 768px (原始设计)
- 平板: 481px - 768px (优化菜单和网格)
- 手机: ≤ 480px (极端优化)
```

### 汉堡菜单
- **触发屏幕:** 屏幕宽度 ≤768px
- **样式:** 三条横线，点击变成 X 形
- **位置:** 导航栏右侧
- **功能:** 显示/隐藏垂直菜单

## 📱 视觉改进

### 导航
```
桌面版：      | 移动版：
Home → About  | ☰ (汉堡菜单)
Programs ...  | ↓ (展开后)
              | Home
              | About
              | Programs ▾
              |   ├─ Skill Dev
              |   ├─ Seminars
              |   └─ Mentorship
```

### 布局改变
| 元素 | 桌面 | 平板 | 手机 |
|------|------|------|------|
| 导航 Logo | 70px | 50px | 40px |
| Hero 标题 | 3.5rem | 1.8rem | 1.4rem |
| 程序卡片网格 | 4列 | 2列 | 1列 |
| 统计网格 | 6列 | 2列 | 1列 |
| 内边距 | 2rem | 1rem | 1rem |

## 🔧 开发信息

### 关键 CSS 类
```css
.mobile-menu-btn          /* 汉堡菜单按钮 */
.mobile-menu-btn.active   /* 激活状态（X形） */
#navMenu.active           /* 菜单打开状态 */
#programsDropdown.active  /* 下拉菜单打开状态 */
```

### JavaScript 事件监听
```javascript
// 汉堡菜单点击
mobileMenuBtn.addEventListener('click')

// 导航项点击
navLinks.forEach(link => {
  link.addEventListener('click')
})

// 下拉菜单点击
dropdownTrigger.addEventListener('click')

// 外部点击
document.addEventListener('click')
```

## ✅ 测试清单

### 在不同设备上测试
- [ ] iPhone 12 (390px)
- [ ] iPhone SE (375px)  
- [ ] iPad (768px)
- [ ] iPad Pro (1024px)
- [ ] Android 手机 (412px)

### 功能检查
- [ ] 汉堡菜单在小屏幕显示
- [ ] 汉堡菜单动画平滑
- [ ] 菜单展开/折叠正常
- [ ] Programs 下拉菜单可用
- [ ] 所有页面可读可用
- [ ] 图片和视频正确缩放
- [ ] 表单可以填写
- [ ] 按钮可以点击

## 📄 更新的文件列表

```
✅ templates/index.html
✅ templates/about.html
✅ templates/contact.html
✅ templates/partner.html
✅ templates/skill-development.html
✅ templates/mentorship.html
✅ templates/seminars.html
✅ templates/environmental-competition.html
✅ app.py (HTML_TEMPLATE)
```

## 🚀 部署说明

1. **本地测试**
   ```bash
   python3 app.py
   ```
   然后在浏览器中用开发者工具的设备模拟器测试

2. **生产部署**
   - 无需额外配置
   - 所有变更都是 CSS/JS，不影响后端
   - 兼容现有的 Vercel 部署

3. **验证**
   - 检查所有页面是否加载正常
   - 在真实设备上测试汉堡菜单
   - 测试各个屏幕尺寸

## 🎨 自定义建议

### 修改汉堡菜单颜色
在 `<style>` 中找到：
```css
.mobile-menu-btn span {
    background: #333; /* 修改这里 */
}
```

### 修改菜单展开宽度
```css
nav ul {
    width: 100%; /* 已是全宽 */
}
```

### 调整断点
修改 `@media` 查询的数值：
```css
@media (max-width: 768px) { /* 修改 768 */
```

## 📞 支持

如有问题，检查：
1. 浏览器控制台是否有 JavaScript 错误
2. CSS 是否在 `<style>` 标签中加载
3. HTML 中是否有正确的 id (`mobileMenuBtn`, `navMenu`, `programsDropdown`)
4. 网络连接是否正常

---
最后更新: 2025-12-28
